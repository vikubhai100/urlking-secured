# URLKING Frontend - Production Environment Variables
# These are baked into the build at compile time (not runtime)

# Backend API URL
VITE_API_URL=https://go.urlking.site

# Short domain for generated links (MUST include https://)
VITE_SHORT_DOMAIN=https://urlking.in
